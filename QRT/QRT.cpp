
#include <windows.h>
#include <GL/GL.h>


LRESULT CALLBACK WndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam);


int WinMain(__in HINSTANCE hInstance, __in_opt HINSTANCE hPrevInstance,
		    LPSTR lpCmdLine, __in int nShowCmd)
{
	return 0;
}


LRESULT CALLBACK WndProc(HWND hWnd, UINT message, WPARAM wParam, LPARAM lParam)
{

}